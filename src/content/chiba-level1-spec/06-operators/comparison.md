# 比较运算

## 语法

当前至少包括：

- `==`
- `!=`
- `<`
- `<=`
- `>`
- `>=`

## 语义

比较运算产生 `bool` 结果，并可能参与结构性 overload。

## Usage

```chiba
let ok = a < b
let same = x == y
```

注释：比较运算的直接结果是 `bool`，因此通常作为 `if`、guard 或其他布尔上下文的输入。

## 边界

需要单独明确：

- 可比较类型的最小集合
- 结构值的比较语义
