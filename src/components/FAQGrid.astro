---
import BentoCard from './BentoCard.astro';

const faqs = [
  { q: 'Tail-Call Opt.', a: 'ATP proves stack depth bounds at compile-time.' },
  { q: 'Uniqueness', a: 'Tracks resources through function boundaries.' },
  { q: 'No LLVM', a: 'Built for deterministic cycle-by-cycle control.' },
  { q: 'Continuations', a: 'Parser resumes from valid sync points locally.' },
];
---

<section class="col-span-1 md:col-span-4 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-0 border-x border-chiba-line">
  {faqs.map((f, index) => (
    <BentoCard class={`bg-chiba-container-low/50 border-y border-chiba-line ${index < faqs.length - 1 ? 'lg:border-r md:border-r-0' : ''}`}>
      <div class="flex items-center justify-between mb-4 border-b border-chiba-line pb-3">
        <h4 class="font-mono text-[11px] font-bold text-chiba-green uppercase tracking-[0.2em]">{f.q}</h4>
        <span class="font-mono text-[10px] text-chiba-gray">0{index + 1}</span>
      </div>
      <p class="text-xs text-chiba-gray leading-relaxed font-medium">{f.a}</p>
    </BentoCard>
  ))}
</section>
