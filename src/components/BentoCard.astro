---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---

<div class={`bg-chiba-container border border-chiba-line p-6 relative overflow-hidden h-full ${className}`}>
  <slot />
</div>
